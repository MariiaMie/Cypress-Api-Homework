name: Cypress Tests

on: push

jobs:
    cypress-run:
        runs-on: ubuntu-22.04
        steps:
            - name: Checkout
              uses: actions/checkout@master
            - name: Cypress run
              uses: cypress-io/github-action@master
              with:
                  build: npm run build
                  start: npm start
